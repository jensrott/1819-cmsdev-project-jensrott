export class SleepResults {
  // id: string;
  day: string;
  sleep_time: string;
  wake_up_time: string;
  // status: string; // Check if the goal is still relevant, yes or no
  // user_id: string;
}
